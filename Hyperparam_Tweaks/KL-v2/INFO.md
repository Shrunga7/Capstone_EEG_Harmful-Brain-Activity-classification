The second version of the KL model.
LR and Dropout were returned to default. Hidden-layers = 128 (halved).
The model still overfitted but did so after 2 epochs.
We saw the loss increase, but also the accuracy for validation?
F1-score and accuracy are the highest of all the models.
